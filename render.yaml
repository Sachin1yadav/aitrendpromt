services:
  - type: web
    name: aitrendpromt-backend
    env: node
    buildCommand: npm ci --production --ignore-scripts || npm install --production --ignore-scripts
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
    plan: free

